#include <stdio.h>

int main() {

    // declaring the variable as an integer
    int a = 1;

    while (a > 5) {
        a++;
        printf("a = %d\n", a);
    }

    printf("After exiting the loop: a = %d\n", a);
    // return 0;

}